language: php

php:
  - 7.1
  - 7.2
  - 7.3

before_script:
  - php -S localhost:8000 index.php &
  - sh -c "composer install --dev --no-progress"

script: vendor/bin/phpunit tests/index.test.php

  
